# Migration `20200115085937-init`

This migration has been generated by Thor Anderson at 1/15/2020, 8:59:37 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Course" (
  "coreq" text  NOT NULL DEFAULT '' ,
  "createdAt" timestamp(3)  NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  "crossListed" text  NOT NULL DEFAULT '' ,
  "description" text  NOT NULL DEFAULT '' ,
  "geCode" text  NOT NULL DEFAULT '' ,
  "id" text  NOT NULL  ,
  "labCredits" text  NOT NULL DEFAULT '' ,
  "lectureCredits" text  NOT NULL DEFAULT '' ,
  "number" text  NOT NULL DEFAULT '' ,
  "prefix" text  NOT NULL DEFAULT '' ,
  "preorco" text  NOT NULL DEFAULT '' ,
  "prereq" text  NOT NULL DEFAULT '' ,
  "subject" text  NOT NULL DEFAULT '' ,
  "termsOffered" text  NOT NULL DEFAULT '' ,
  "title" text  NOT NULL DEFAULT '' ,
  "totalCredits" text  NOT NULL DEFAULT '' ,
  "updatedAt" timestamp(3)  NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  PRIMARY KEY ("id")
);

CREATE TABLE "public"."Competency" (
  "createdAt" timestamp(3)  NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  "id" text  NOT NULL  ,
  "updatedAt" timestamp(3)  NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  PRIMARY KEY ("id")
);
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200115085937-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,35 @@
+datasource postgres {
+  provider = "postgresql"
+  url      = "postgresql://postgres:docker@localhost:5432/pg-comp-model?schema=public"
+}
+
+generator photon {
+  provider = "photonjs"
+}
+
+model Course {
+  id        String   @default(cuid()) @id
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  coreq     String
+  crossListed String
+  description String
+  geCode    String
+  labCredits  String
+  lectureCredits  String
+  number  String
+  prefix  String
+  preorco String
+  prereq  String
+  subject String
+  termsOffered  String
+  title String
+  totalCredits  String
+}
+
+model Competency {
+  id        String   @default(cuid()) @id
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  
+}
```


